{% for flash in app.flashes('sweetalert_error') %}
    <script>
        Swal.fire({
            icon: 'error',
            title: '{{ flash.message }}',
            showConfirmButton: true,
        });
    </script>
{% endfor %}